Vendored from `qrcode-terminal` 0.11.0 for offline QR rendering.

The package is Apache-2.0; its QRCode generator is Kazuhiko Arase's MIT-licensed
QRCode for JavaScript. The CommonJS source files were renamed from `.js` to
`.cjs` so they remain CommonJS under this application's ESM package boundary.
